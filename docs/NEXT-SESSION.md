# Next session — start here

**Last session:** 2026-08-05
**Branch:** `docs/brand-system`
**Programme:** see `docs/REBUILD.md` — this is a ground-up rebuild of Rogue Drones, not a
website rebuild. It was rescoped on 2026-08-05.

---

## Say this to start

> Read `docs/REBUILD.md`, `docs/FOUNDATIONS.md` and `docs/NEXT-SESSION.md`.
> [then name the workstream you want to work on]

---

## Where the programme is

| # | Workstream | Status |
|---|---|---|
| 1 | Identity | Spec approved, Phil's review still owed |
| 2 | Language | **Current** — motto in progress |
| 3 | Presentation (incl. website) | Not started |
| 4 | Winning work | Not started |
| 5 | Client data | Not started |

---

## Last session (2026-08-05)

- Confirmed **Cloudflare Pages** is what actually serves roguedrones.co.nz, not GitHub
  Pages. DNS points at Cloudflare and the live CSP is the one generated by
  `createHeadersFile()` in `build-optimized.js`, which GitHub Pages cannot serve. The
  workflow at `.github/workflows/deploy.yml` and the root `CNAME` are dead — delete them
  during the rebuild.
- Rescoped the whole project from "website rebuild" to "rebuild the organisation".
  Documentation rewritten to match.
- Set out to write a motto. Got much further down than that and settled the two things the
  motto has to compress. **Purpose: "People think they are being productive."** **Vision: a
  world where humans understand their place** — recorded in full in `docs/VISION.md`, which
  is primary source and worth reading before writing any copy. Both in `docs/FOUNDATIONS.md`.
- Found that the kārearea in brand spec §2.4, the offload-and-climb thesis in the F500 primer,
  and the domed-city vision are the same idea arrived at three times independently.
- Recorded the stopping-rule constraint in `REBUILD.md` — it shapes how every later spec
  should be written.

## Session before (2026-08-02)

- Designed and approved the identity system. It went far deeper than a palette: the
  drone/rogue thesis in §2 is what the rest of the programme reads from.

---

## Open

1. **Phil's review of the identity spec.** Three items were decided without him and
   flagged for pushback: `--slate-dark` as a seventh token, form field borders using
   `--slate` not `--silver`, and all of §8 — spacing scale, 12-column grid, motion. The
   load-bearing rule in §8 is border radius 0, no shadows, no gradients, no icon font.
2. **Logo as SVG.** Only PNGs exist. Designer holds the artwork and is swamped; her work
   wins over the spec. Two existing states are enough to build on.
3. **No case-study content exists.** No project locations, no screenshots, no outcomes,
   no record of which clients can be named. Workstream 3 is blocked on this and it is
   writing, not code.
4. **Website map treatment** — monochrome and abstract, or true-colour satellite as the
   single loudest thing on a silent page. Parked deliberately; it is a workstream 3
   decision.
5. **Website framework.** Not discussed. Bias toward the least machinery that gives a
   shared layout and self-hosted fonts.
6. **Whether the current site is deleted or kept.** Phil is happy to scrap it entirely.

---

## Next move

**The mission.** It is the falsifiable one — what Rogue Drones is doing about the purpose,
now, with a horizon and a way to fail — and nothing downstream can be tested without it.

It is blocked on one decision, raised on 2026-08-05 and not answered: whether Rogue Drones
stays with the six clients and the next twenty, pursues the pattern at enterprise scale as
sketched in the F500 primer, or treats the six as how it earns the right to the pattern.
Those are three different businesses and they produce three different missions.

Values and the motto come after. The motto is a compression of the rest and cannot be written
first — that was tried on 2026-08-05 and every attempt came out as a slogan with nothing
underneath it.
